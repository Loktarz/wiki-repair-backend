spring.application.name=repair_system_db

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/repair_system_db}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:Loktarpostgres11}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Secret key: must be Base64-encoded, at least 256 bits (32 bytes) for HS256
jwt.secret=${JWT_SECRET:bXlTdXBlclNlY3JldEtleUZvclJlcGFpclRpY2tldFN5c3RlbTIwMjQ=}
# Token expiration: 24 hours in milliseconds (24 * 60 * 60 * 1000)
jwt.expiration=86400000

logging.level.org.springframework.security=WARN

# ─── Email (Gmail SMTP) ────────────────────────────────────────────────────────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:wikirepair.notifications@gmail.com}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
app.mail.from=Wiki Repair <wikirepair.notifications@gmail.com>